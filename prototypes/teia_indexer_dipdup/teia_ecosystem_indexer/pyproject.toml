# generated by DipDup 8.5.1
[project]
name = "teia_ecosystem_indexer"
version = "0.0.1"
description = "Teia and h=n token and transaction indexer built on dipdup"
license = { text = "MIT" }
authors = [
    { name = "Kyle Grover", email = "kyle@kylegrover.com" },
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "dipdup>=8,<9",
]

[dependency-groups]
dev = [
    "ruff>=0.9.2",
    "mypy>=1.14.1",
]

[tool.ruff]
line-length = 120
target-version = 'py312'

[tool.ruff.lint]
extend-select = ["B", "C4", "FA", "G", "I", "PTH", "RET", "RUF", "TCH", "UP"]
isort = { force-single-line = true}

[tool.ruff.format]
quote-style = "single"

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
explicit_package_bases = true
strict = false
exclude = "teia_ecosystem_indexer"

[[tool.mypy.overrides]]
module = "ruamel"
ignore_missing_imports = true

[tool.hatch.build.targets.wheel]
packages = ["teia_ecosystem_indexer"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"